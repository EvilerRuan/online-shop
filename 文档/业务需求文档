业务需求文档

1. 项目概述
开发一个线上批发商城 H5 系统，包含用户端（顾客选品下单）和管理后台（运营管理）。用户端通过手机浏览器、App 内嵌 WebView、小程序 web-view 访问。管理后台通过 PC 浏览器访问。系统基于 Supabase（数据库、认证、存储）和 Cloudflare（部署、事务 API）构建。

1.1 全局规则
强制登录：未登录用户无法访问任何页面，打开应用即跳转登录页。

账号来源：所有账号由管理后台预先添加（手机号+初始密码），不支持自助注册。

三端共用：H5 代码嵌入 Android/iOS WebView 以及小程序 web-view。

图标占位：所有需要图标的地方（金刚区、Banner 暂无图标问题）先由 AI 生成一个简单的 SVG 或 Emoji 占位，后期管理员可自行替换上传。

2. 用户端功能需求
2.1 登录页
输入手机号、密码，调用 Supabase 认证登录。

登录成功后跳转首页，保存登录态（Supabase Session）。

错误提示：账号不存在、密码错误、账号被禁用等。

2.2 首页
顶部搜索框：点击进入搜索页（独立页面）。

Banner 轮播图：支持自动轮播，点击跳转配置链接（商品详情/分类/外部链接）。图片由管理后台上传。

金刚区图标区：最多展示 10 个入口（爆品、新品、一元区等）。每个入口包含：图标（AI 生成占位，后期替换）、名称、跳转链接。图标暂时用纯色背景 + Emoji 或简单文字代替，管理员可后续在后台修改图标。

热销推荐商品列表：垂直滚动列表（上下滚动），每行展示 2 个商品（网格模式）。展示商品图片、名称、价格、销量。点击商品跳转详情页；点击商品上的“购物车”图标直接跳转详情页。

下拉刷新：支持下拉刷新首页数据。

2.3 分类页
左侧：一级分类列表（全部、一元区、处理区……），数据来自管理后台配置（parent_id=0 且 show_in_client=true）。没有图标，仅显示文字。

默认选中第一个一级分类。

右侧区域：

如果当前选中的一级分类存在二级分类，则右侧显示该一级分类下的所有二级分类（网格布局，每个分类显示名称，无图标）。点击某个二级分类，跳转到商品列表页（筛选条件：该二级分类）。

如果当前选中的一级分类没有二级分类，则右侧不显示子分类区域，并且点击该一级分类时直接跳转到商品列表页（筛选条件：该一级分类）。

注：分类本身不设图标，所有图标仅为金刚区等运营位使用。

2.4 商品列表页（独立页面）
顶部显示当前分类名称。

支持排序：综合（默认）、价格升序、价格降序、销量降序。

商品网格展示（图片、名称、价格、销量），每个商品带“购物车”图标，点击跳转详情页。

分页加载（滚动到底部自动加载更多）。

2.5 搜索页
输入关键词（商品名称或条码），实时模糊搜索。

搜索结果以商品网格展示，支持排序（同商品列表页）。

点击商品跳转详情页。

2.6 商品详情页
信息区域：商品主图、商品名称、商品编号、条码、销量、单价、最低订购量、库存数量。

规格选择：如果商品有多个规格（个/包/件），以按钮组形式展示，默认选中第一个。切换规格时，下方的单价、最低订购量、库存数量同步更新。

加入购物车按钮：点击后弹出底部弹框（ActionSheet）。

弹框内容：规格切换（同详情页规格按钮）、数量选择器（加减按钮）、单价、购买数量、总金额、备注输入框（可选）。

数量选择器限制：最小值为当前规格的“最低订购量”，最大值为当前规格的“库存”。步长为 1。

每次增减数量时，自动重新计算总金额（单价 × 数量）。

点击“确定”后：将商品（含所选规格和数量）加入购物车；如果同一规格已存在，则累加数量（不超过库存）；弹出“添加成功”提示，关闭弹框。

商品详情描述：展示富文本内容。

2.7 购物车页
列表展示所有加购的商品（无店铺分组），每个商品显示：图片、名称、规格名称、单价、数量、小计金额。

修改数量：点击数量旁边的“+”“-”按钮，实时更新数量（校验不低于最低订购量，不超过库存），重新计算小计和底部总价。

删除商品：点击删除图标，弹框二次确认，确认后从购物车移除。

底部栏：显示已选商品总金额（所有商品之和），一个“结算”按钮。

全选/取消全选（建议实现）：用户可单独勾选商品结算，默认全选。

点击“结算”：跳转到确认订单页（携带当前选中商品）。

2.8 确认订单页
收货地址：展示当前默认地址，可点击切换/新增地址（跳转地址管理页）。

商品清单：展示本次结算的商品（图片、名称、规格、数量、小计）。

订单总金额：所有商品小计之和。

备注：文本输入框（选填）。

提交订单按钮：

调用后端 API（Cloudflare Worker）校验库存、扣减库存、生成订单、清空购物车。

成功后跳转到订单列表页，并弹出“订单已提交”提示。

如果库存不足，提示“xxx商品库存不足”并停留在当前页。

2.9 收货地址管理
地址列表：展示所有地址（收件人、电话、详细地址）。每个地址右侧有“编辑”和“删除”图标。删除需二次确认。

设为默认：每个地址可设为默认，默认地址在列表中置顶。

新增/编辑地址：

字段：收件人（必填）、联系电话（必填，手机号校验）、省/市/区（三级联动，使用静态数据）、详细街道（必填）。

可选“设为默认地址”复选框。

保存后返回地址列表。

2.10 个人中心页
用户信息卡片：显示用户名、用户编号（无头像）。

我的订单区域：

五个快捷入口：全部订单、待付款、待发货、待收货（显示各状态数量角标）。

点击进入订单列表页（默认选中对应状态）。

地址管理：入口跳转地址管理页。

商家简介、商家公告、买家须知：三个静态页面，内容由管理后台编辑（富文本）。

退出登录：点击后清除本地登录态，跳转登录页。

2.11 订单列表页
顶部 Tab 切换：全部、待付款、待发货、待收货、已完成、已取消。

列表按订单创建时间倒序，每个订单显示：订单号、下单时间、总金额、订单状态、商品缩略图（最多3个）。

点击订单进入订单详情页。

对于“待付款”状态的订单，显示“取消订单”按钮（二次确认），取消后订单状态变为“已取消”，并恢复库存。

对于“待收货”状态的订单，显示“确认收货”按钮，确认后状态变为“已完成”。

2.12 订单详情页
展示订单信息：订单号、下单时间、订单状态、收货地址（收件人、电话、地址）。

商品清单：名称、规格、单价、数量、小计。

订单总金额、备注。

底部按钮：根据状态显示“取消订单”、“确认收货”等操作。