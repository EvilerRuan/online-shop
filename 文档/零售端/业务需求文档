零售商城业务需求文档

1. 项目概述
在现有批发商城系统（Monorepo，pnpm workspaces）基础上扩展开发 B2C 零售商城。系统与现有批发商城共享数据库、API 层和部分管理功能，通过渠道标识（channel）区分批发/零售业务。

零售端用户端采用 Taro 4.x + Vue 3 + NutUI 4.x + Pinia 开发，同时支持 H5 和微信小程序两个平台。管理后台在现有 React + Ant Design 后台基础上扩展零售管理模块。

1.1 全局规则
渠道隔离：profiles/products/orders 通过 channel/sales_channel 字段区分批发和零售，查询时务必过滤。

模拟支付：下单后直接标记为支付成功，订单进入 pending_shipment 状态，无需对接真实支付。

最小购买量：零售商品无最低订购量，最小购买 1 件。

登录方式：微信小程序一键登录（wx.login）+ 手机号验证码登录（模拟验证码，固定 123456）。

积分体系：注册赠送 + 推荐新人赠送 + 首次确认收货赠送（一次性），积分可兑换积分商城商品，不支持下单抵扣。

售后退款：模拟原路退回，记录退款状态即可，不实际执行资金操作。

客服聊天：消息通过 HTTP POST 持久化到 customer_messages 表，WebSocket 推送为增强功能（可后续接入）。

2. 零售端用户端功能需求

2.1 登录/注册页
微信小程序登录：仅小程序环境可用，调用 wx.login 获取 code → 后端 code 换 openid → 查找/创建用户 → 返回 JWT。

手机号验证码登录：H5 和小程序均可用，输入手机号 → 发送验证码（模拟，固定 123456） → 输入验证码 → 登录/注册。

首次登录自动创建零售用户（channel=retail），赠送注册积分。

如果携带推荐码（URL 参数 ref），自动建立推荐关系，奖励推荐人积分。

平台判断：process.env.TARO_ENV === 'weapp' 时显示微信登录按钮，H5 模式仅显示验证码登录。

2.2 首页（TabBar 第1个）
顶部搜索框：点击进入搜索页。

Banner 轮播图：数据来自 retail_banners 表，支持自动轮播，点击跳转配置链接。

金刚区图标区：数据来自 retail_quick_icons 表，展示入口图标和名称，点击跳转对应页面。

热销推荐商品列表：数据来自 retail_home_products 关联表，网格 2 列展示，展示商品图片、名称、零售价、销量。点击商品跳转详情页。

下拉刷新：支持下拉刷新首页数据。

2.3 分类页（TabBar 第2个）
左侧：一级分类列表，数据来自 categories 表（parent_id=0 且 show_in_client=true），仅显示文字。

默认选中第一个一级分类。

右侧区域：
如果当前选中的一级分类存在二级分类，则右侧显示二级分类网格。点击二级分类，跳转商品列表页。
如果当前选中的一级分类没有二级分类，则右侧不显示子分类区域，点击直接跳转商品列表页。

2.4 购物车页（TabBar 第3个）
列表展示所有加购商品，每个商品显示：图片、名称、规格名称、零售价、数量、小计金额。

修改数量：加减按钮，最小 1 件，最大为库存数量。实时更新小计和底部总价。

删除商品：滑动删除或点击删除图标，二次确认。

底部栏：显示已选商品总金额 + 结算按钮。

全选/取消全选：用户可单独勾选商品结算，默认全选。

点击"结算"：跳转确认订单页。

未登录时购物车为空提示，引导登录。

2.5 个人中心页（TabBar 第4个）
用户信息卡片：显示头像（avatar_url）、用户名、手机号。

订单快捷入口：全部订单、待发货、待收货、已完成（显示各状态数量角标）。

功能入口列表：
地址管理：跳转地址管理页
售后管理：跳转售后列表页
积分中心：跳转积分中心页
推荐好友：跳转推荐好友页（分享携带推荐码）
客服聊天：跳转客服聊天页

退出登录：清除本地登录态，跳转登录页。

2.6 搜索页（Push 页面）
输入关键词（商品名称），实时模糊搜索。

搜索结果以商品网格展示，支持排序（综合、价格升降序、销量降序）。

点击商品跳转详情页。

仅展示 sales_channel 为 'retail' 或 'both' 的商品。

2.7 商品列表页（Push 页面）
顶部显示当前分类名称或搜索关键词。

支持排序：综合（默认）、价格升序、价格降序、销量降序。

商品网格展示（图片、名称、零售价、销量），点击跳转详情页。

分页加载（滚动到底部自动加载更多）。

仅展示 sales_channel 为 'retail' 或 'both' 的 active 商品。

2.8 商品详情页（Push 页面）
信息区域：商品主图、商品名称、零售价、销量、库存数量。

规格选择：如果商品有多个规格，以按钮组形式展示，默认选中第一个。切换规格时同步更新价格和库存。

加入购物车：点击后弹出底部弹框，包含规格选择、数量选择器（最小1，最大库存）、单价、总金额、备注输入框。点击"确定"加入购物车。

商品详情描述：展示富文本内容。

零售价展示：使用红色醒目价格，与批发端区分。

无最低订购量限制，数量选择器最小值为 1。

2.9 确认订单页（Push 页面）
收货地址：展示当前默认地址，可点击切换/新增地址。

商品清单：展示本次结算的商品（图片、名称、规格、数量、小计）。

运费信息：根据收货地址自动计算运费（精确匹配省+市 → 省级 → 默认运费），展示运费金额。满额免运费提示。

订单总金额：商品合计 + 运费。

备注：文本输入框（选填）。

提交订单按钮：
调用后端 API 校验库存、扣减库存、计算运费、生成订单（channel=retail）、清空购物车中已结算商品。
下单即支付成功，订单直接进入 pending_shipment 状态。
成功后跳转订单列表页。
如果库存不足，提示并停留在当前页。

2.10 地址管理（Push 页面）
地址列表：展示所有地址（收件人、电话、详细地址）。每个地址有"编辑"和"删除"图标。

设为默认：每个地址可设为默认，默认地址置顶。

新增/编辑地址：
字段：收件人（必填）、联系电话（必填，手机号校验）、省/市/区（三级联动）、详细街道（必填）。
可选"设为默认地址"。
保存后返回地址列表。

2.11 订单列表页（Push 页面）
顶部 Tab 切换：全部、待发货、待收货、已完成、已取消。

列表按订单创建时间倒序，每个订单显示：订单号、下单时间、总金额（含运费）、运费、订单状态、商品缩略图。

点击订单进入订单详情页。

对于"待发货"状态的订单：显示"取消订单"按钮（二次确认），取消后恢复库存。

对于"待收货"状态的订单：显示"确认收货"按钮 + "申请售后"按钮。确认后状态变为 completed，首次确认收货赠送积分。

2.12 订单详情页（Push 页面）
展示订单信息：订单号、下单时间、订单状态、收货地址。

商品清单：名称、规格、单价、数量、小计。

运费信息、订单总金额。

备注。

底部按钮：根据状态显示"取消订单"、"确认收货"、"申请售后"等操作。

2.13 售后申请页（Push 页面）
选择售后类型：仅退款 / 退货退款。

填写退款原因（文本输入）。

填写退款金额（不超过商品金额）。

上传凭证图片（可选）。

提交后生成售后工单，状态为 pending。

2.14 售后列表页（Push 页面）
展示用户的所有售后工单列表。

每个工单显示：订单号、售后类型、状态、退款金额、申请时间。

点击工单进入售后详情页。

2.15 售后详情页（Push 页面）
展示售后工单详细信息：工单号、关联订单、售后类型、状态、退款金额、申请原因、凭证图片、申请时间。

状态流转展示：
仅退款：pending → approved → completed / pending → rejected
退货退款：pending → processing → processing(用户填单号) → completed / processing → rejected

操作按钮（根据状态）：
processing 状态：填写退货快递单号
rejected 状态：展示拒绝原因

2.16 客服聊天页（Push 页面）
顶部显示"联系客服"。

消息列表：气泡样式，用户消息靠右，客服消息靠左。

底部输入框 + 发送按钮。

支持文字消息和图片消息。

消息通过 HTTP POST 持久化，可选 WebSocket 实时推送（降级为 HTTP 轮询）。

进入页面自动发送欢迎消息（系统自动回复）。

2.17 积分中心页（Push 页面）
显示当前积分余额。

积分获取规则说明（注册赠送/推荐赠送/首次收货赠送）。

积分流水列表（earn/spend 分类，按时间倒序）。

入口：积分商品列表。

2.18 积分商品列表页（Push 页面）
网格展示积分兑换商品：图片、名称、所需积分、库存。

点击进入积分商品详情页。

2.19 积分商品详情页（Push 页面）
商品图片、名称、所需积分、库存数量。

兑换按钮：点击后校验积分余额是否充足 + 库存是否充足，确认后扣除积分生成兑换订单。

2.20 推荐好友页（Push 页面）
显示推荐码（用户 ID 或专用码）。

分享按钮（仅小程序环境）：调用微信分享 API，携带推荐码。

H5 环境降级：显示推荐码 + 复制链接按钮。

推荐历史列表：展示已推荐的用户和获得的积分奖励。

3. 管理后台扩展功能需求

3.1 Dashboard 扩展
在现有 Dashboard 基础上增加渠道切换器（全部/批发/零售），切换后统计数据随之变化。

零售模式下额外显示：
待处理售后数量
待回复客服消息数量

3.2 商品管理扩展
商品编辑表单增加：
零售价字段（retail_price，DECIMAL）
销售渠道字段（sales_channel：wholesale/retail/both）

商品列表增加渠道筛选。

3.3 订单管理扩展
订单列表顶部增加 Tab 切换：全部订单 / 批发订单 / 零售订单。

零售订单列表增加运费列。

3.4 用户管理扩展
分为"批发用户"和"零售用户"两个入口（Tab 或菜单分离）。

零售用户列表额外显示：积分余额、推荐人数。

零售用户详情支持手动调整积分（增加/扣减 + 原因）。

3.5 首页配置扩展
分为"批发端配置"和"零售端配置"两个 Tab。

零售端配置管理：
轮播图（retail_banners 表）
金刚区图标（retail_quick_icons 表）
热销推荐商品（retail_home_products 表）

3.6 运费管理（新增模块）
城市运费 CRUD 列表：
表格字段：省份、城市、运费类型（fixed/free_threshold/free）、基础运费、免运费阈值、是否启用。

新增/编辑运费配置表单。

默认运费设置（写入 system_settings 表）。

运费计算规则说明：精确匹配省+市 → 匹配省+city='' → 默认运费。

3.7 积分管理（新增模块，4个子页）
积分规则：KV 配置表单（register_points/referral_points/first_purchase_points）。

积分商品：CRUD 列表（名称、图片、所需积分、库存、是否启用）。

兑换记录：列表展示所有积分兑换订单（用户、商品、积分、状态），支持发货操作。

积分流水：列表展示所有积分变动记录（用户、类型、原因、金额、余额）。

3.8 售后管理（新增模块）
工单列表：Tab 切换（全部/待处理/处理中/已完成/已拒绝/已关闭）。

表格字段：工单号、订单号、用户、售后类型、状态、退款金额、申请时间。

工单详情页：
展示工单完整信息 + 关联订单信息。

操作按钮：
pending 的仅退款：同意 / 拒绝
pending 的退货退款：同意（进入 processing）
processing 的退货退款：确认收货退款 / 拒绝
确认操作需二次确认弹框。

3.9 客服消息（新增模块）
左右分栏布局：
左侧：用户列表（最近聊天的用户 + 未读消息数）。
右侧：选中用户的聊天记录。

支持发送文字消息回复用户。

可选 WebSocket 实时更新，降级为 HTTP 轮询（定时刷新消息列表）。

3.10 系统设置扩展
在现有系统设置基础上新增：
积分配置（积分规则数值）
默认运费值
客服自动回复语
